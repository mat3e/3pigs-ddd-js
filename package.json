{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "author": "mat3e",
  "scripts": {
    "test": "jest",
    "build": "lerna run build",
    "publish": "npm run build && lerna publish",
    "bootstrap": "lerna bootstrap --force-local"
  },
  "devDependencies": {
    "jest": "^27.4.3",
    "lerna": "^4.0.0"
  }
}
